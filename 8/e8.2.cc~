#include <iostrean>
using std::istream;
using std::cin;
using std::cout;

istrean& func(istream &strm) {
  string s;
  while (strm >> s) {
    cout << s;
  }
  strm.clear();
  return strm;
}









